#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    void sortColors(vector<int>& nums) {
        sort(nums.begin(), nums.end());
    }
};

/*
Time Complexity: O(n log n)
Extra Space: O(log n)
Approach: General sorting.
*/
