#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    void sort012(vector<int>& arr) {
        sort(arr.begin(), arr.end());
    }
};

/*
Time Complexity: O(n log n)
Extra Space: O(log n)
Approach: General sorting.
*/
