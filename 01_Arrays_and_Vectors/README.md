# 01 — Arrays & Vectors

Status: **completed reference module**

This module preserves the original 80-problem Arrays/Vectors curriculum, including every personal attempt slot, brute-force/better/optimal reference, beginner explanation, mistake log, test case, revision note, metadata file, executable oracle, and validation rule.

The ten folders below are internal learning patterns, not separate top-level textbook modules. Start with linear traversal and continue in numbered order through matrices.

Run the full verified reference suite from the repository root:

```bash
python3 scripts/validate_structure.py
python3 scripts/test_solutions.py --sanitize
```

The permanent `topic/arrays-vectors` branch records this module immediately before it was moved under the cumulative textbook structure.
